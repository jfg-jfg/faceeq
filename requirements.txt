# FaceEQ v0.2.0 运行必需（纯协议 EQ 中间件，产品 exe 只打包这些）
websocket-client>=1.6.0   # VTS WebSocket 注入
python-osc>=1.8.0         # VMC/OSC 输出 + VMC 输入（Warudo/VSeeFace 等生态）
Pillow>=10.0.0            # 校正向导 PIL 中英文文字渲染
PySide6>=6.6.0            # GUI 控制面板（VTS 风格）
sounddevice>=0.4.6        # 语音情绪引擎（实验；麦克风采集，wheel 自带 PortAudio）
numpy>=1.24               # sounddevice PCM → float32（voice.py）
opencv-python>=4.8.0      # 校正向导 cv2 窗口（faceeq/calibrate.py）

# ── 以下仅本地开发需要（git clone 后开发/调试用），产品 exe 不包含 ──
# live2d-py>=0.7.0        # （已删 render.py 预览，保留供历史参考）
# glfw>=2.7
# PyOpenGL>=3.1.7
